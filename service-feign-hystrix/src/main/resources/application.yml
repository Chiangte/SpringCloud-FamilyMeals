#指定端口号
server:
  port: 8765
#指定注册地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#指定程序名
spring:
  application:
    name: service-feign
#打开断路器
feign.hystrix.enabled: true
